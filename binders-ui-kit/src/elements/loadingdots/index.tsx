import * as React from "react";
import "./loadingdots.styl";

const LoadingDots = (): JSX.Element => (
    <p className="loading">
        <span>.</span>
        <span>.</span>
        <span>.</span>
    </p>
);
export default LoadingDots;