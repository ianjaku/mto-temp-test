import * as React from "react";
import Icon from "../";

export const ICON_NAME = "compare_arrows";

const compareArrows = (style = {}): JSX.Element => <Icon name={ICON_NAME} style={style} />;

export default compareArrows;
