import * as React from "react";
import Icon from "../";

export const ICON_NAME = "language";

const language = (style = {}): JSX.Element => <Icon name={ICON_NAME} style={style} />;

export default language;
