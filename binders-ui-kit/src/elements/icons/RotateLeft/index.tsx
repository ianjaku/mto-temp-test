import * as React from "react";
import Icon from "../";

export const ICON_NAME = "rotate_left";

const rotateLeft = (style): JSX.Element => <Icon name={ICON_NAME} style={style} />;

export default rotateLeft;
