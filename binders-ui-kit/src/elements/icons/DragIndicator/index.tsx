import * as React from "react";
import Icon from "../";

export const ICON_NAME = "drag_indicator";

const menu = (style = {}): JSX.Element => <Icon name={ICON_NAME} style={style} />;

export default menu;
