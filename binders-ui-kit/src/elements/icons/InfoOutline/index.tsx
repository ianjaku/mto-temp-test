import * as React from "react";
import Icon from "../";

export const ICON_NAME = "info_outline";

const infoOutline = (style = {}): JSX.Element => <Icon name={ICON_NAME} style={style} />;

export default infoOutline;
